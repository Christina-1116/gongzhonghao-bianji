# Upload Ready Bundle

This folder is generated by `archive_wechat_package.py`.

- Use `upload-manifest.json` as the single source of truth for metadata and image order.
- Use `wechat-publish.html` as the article body source.
- Upload `cover.bundle_path` first when the later draft uploader needs the thumb image.
